const Settings = {
  rentOffersCount: 5,
  isSignedIn: true,
};

const locations: string[] = [
  'Paris', 'Cologne', 'Brussels', 'Amsterdam', 'Hamburg', 'Dusseldorf'
];
export { Settings, locations };
